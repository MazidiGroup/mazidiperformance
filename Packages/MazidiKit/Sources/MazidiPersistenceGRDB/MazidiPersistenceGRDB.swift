// MazidiPersistenceGRDB — durable SQLite store behind the MazidiPersistence contracts.
// The ONLY target permitted to import GRDB (ADR-0007). Schema and migration policy are
// documented in docs/architecture/MIGRATIONS.md.
import GRDB
