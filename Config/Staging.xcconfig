#include "Base.xcconfig"

// Staging — pre-production builds. A staging media origin is injected here once a
// staging CDN exists (R-02); until then MEDIA_BASE_URL stays empty and the remote
// media tier is inert (ADR-0011 §4 tier 3). Never commit a real host/secret inline.
